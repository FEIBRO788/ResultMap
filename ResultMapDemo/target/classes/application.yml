
#????????url?username?password?????????
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tedu?serverTimezone=Asia/Shanghai&characterEncoding=utf8&serverTimeZone=Asia/Shanghai
    username: root
    password: root
#??????,??????????????????????(resultType)
#subject_id ? subjectId
mybatis:
  configuration:
    map-underscore-to-camel-case: true  # 启用驼峰映射（取消注释）
  mapper-locations: classpath:mappers/*.xml  # 同级缩进
  type-aliases-package: cn.tedu.pojo

#????
logging:
  level:
    cn.tedu.mybatisdemo: debug

